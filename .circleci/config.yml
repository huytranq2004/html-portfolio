version: 2.1

jobs:
  build:
    docker:
      - image: cimg/node:18.16
    steps:
      - checkout
      - run: npm install
      - run: npm run build

workflows:
  version: 2
  build-and-deploy:
    jobs:
      - build
